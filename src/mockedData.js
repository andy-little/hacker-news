export const mockedPage = [];
for (let i = 1; i <= 8; i++) {
    mockedData.push({
        author: `author${i}`,
        created_at: `created at${i}`,
        num_comments: `num comments${i}`,
        objectID: `id${i}`,
        title: `title${i}`,
        url: `https://www.url${i}.com`,
        points: i,
    });
}
